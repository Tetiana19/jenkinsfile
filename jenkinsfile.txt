#!groovy
// Check
properties([disableConcurrentBuilds()])

pipeline {
     agent { 
        label 'master'
        }
     triggers { pollSCM('*****')}
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))  	//save last 10 builds
        timestamps()
    }
    stages {
        stage("First step") {
            steps {
                echo "HI"
            }
        }
        stage("Second step") {
           steps {
                echo "trigger done success"
           }
       }
    }
}
